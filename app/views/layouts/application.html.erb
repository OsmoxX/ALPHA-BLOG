<!DOCTYPE html>
<html>
<head>

  <title><%= content_for(:title) || "Learing App" %></title>
  <meta name="viewport" content="width=device-width,initial-scale=1">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="mobile-web-app-capable" content="yes">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <%= yield :head %>

  <link rel="icon" href="/icon.png" type="image/png">
  <link rel="icon" href="/icon.svg" type="image/svg+xml">
  <link rel="apple-touch-icon" href="/icon.png">

  <%# Ładuje wszystkie style z app/assets/stylesheets (Bootstrap jest importowany w JS) %>
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
    crossorigin="anonymous">
  <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>

  <%# Ładuje importmap (Turbo, Bootstrap, Popper itp.) %>
  <%= javascript_importmap_tags %>
</head>

<body class="d-flex flex-column min-vh-100">
  <%= render 'layouts/navigation' %>
  <% flash.each do |name, msg| %>
    <div class="alert alert-info m-3"><%= msg %></div>
  <% end %>

  <div class="container mt-4">
    <%= yield %>
  </div>

  <%= render 'layouts/footer' %>

</body>
</html>